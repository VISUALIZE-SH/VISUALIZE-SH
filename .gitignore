# dependencies
node_modules

# build output
dist
dist-ssr
artifacts
/public/previews/
/output/playwright/
/.playwright-cli/
/tmp/

# editor / OS
.DS_Store
*.local
.vite
*.log

# local configuration and credentials (the empty template is intentionally public)
.env
.env.*
!.env.example

# Claude Code local settings — per-machine tool-permission allowlist, not shared
# (the `*.local` rule above misses the `.local.json` suffix, so list it explicitly)
.claude/settings.local.json

# NOTE: public/graph.json is intentionally NOT ignored — it is generated from
# data/*.yaml and committed so GitHub Pages can serve it without a build server.
